# CPU build — Windows without an NVIDIA card (incl. AMD-GPU boxes) and
# Linux. No CUDA wheels: ctranslate2 runs on the CPU. Slower, so the
# default model is the fastest (large-v3-turbo).
# Install: pip install -r requirements-cpu.txt
faster-whisper==1.1.1
requests>=2.31            # faster-whisper 1.1.1 utils.py imports it for model download
numpy<2.2                 # ctranslate2 / onnxruntime ABI constraint

# Mic capture + hotkey + injection. On Linux: sounddevice needs the
# system PortAudio lib (libportaudio2); pyperclip needs a clipboard
# tool (wl-clipboard on Wayland, xclip/xsel on X11). See README.
sounddevice>=0.4,<0.6
pynput>=1.7,<2.0
pyperclip>=1.8,<2.0       # --paste mode (required on Wayland)

# Wayland (Ubuntu 24.04+): evdev enables global hotkeys by reading
# /dev/input/event* directly (requires the user to be in the 'input'
# group — see README).
evdev>=1.6,<2.0; sys_platform == "linux"
