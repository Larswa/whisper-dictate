# GPU build — Windows with an NVIDIA card (CUDA).
# Install: pip install -r requirements-gpu.txt
faster-whisper==1.1.1
requests>=2.31            # faster-whisper 1.1.1 utils.py imports it for model download
numpy<2.2                 # ctranslate2 / onnxruntime ABI constraint

# CUDA runtime libs ctranslate2 needs (cublas/cudnn). voice_pi.py
# registers their nvidia\*\bin dirs on the DLL path at startup.
nvidia-cublas-cu12
nvidia-cudnn-cu12==9.*

# Mic capture + hotkey + injection.
sounddevice>=0.4,<0.6
pynput>=1.7,<2.0
pyperclip>=1.8,<2.0       # --paste mode
